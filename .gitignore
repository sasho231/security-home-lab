# Secrets — never commit these
*.env
.env*
secrets/
*password*
*credentials*
*.pem
*.key
*.pfx
kubeconfig
# Terraform state contains resource IDs
terraform.tfstate
terraform.tfstate.backup
.terraform/
*.tfvars
# Wazuh docker generates certs locally
wazuh-docker/
# OS junk
.DS_Store
Thumbs.db
